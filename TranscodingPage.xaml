<UserControl x:Class="MooseTracks.Views.TranscodingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppBackground}"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">

    <UserControl.Resources>
        <Style x:Key="SectionGroupBox" TargetType="GroupBox">
            <Setter Property="Foreground" Value="{DynamicResource AppForeground}"/>
            <Setter Property="Background" Value="{DynamicResource BoxesBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid SnapsToDevicePixels="True">
                            <Border CornerRadius="6"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="Transparent"
                                    BorderThickness="0"
                                    Padding="{TemplateBinding Padding}">
                                <Grid Margin="10">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0"
                                               Text="{TemplateBinding Header}"
                                               Foreground="{DynamicResource AppForeground}"
                                               FontWeight="Bold"
                                               Margin="0,0,0,6"/>
                                    <ContentPresenter Grid.Row="1" ContentSource="Content"/>
                                </Grid>
                            </Border>
                            <Rectangle RadiusX="6" RadiusY="6"
                                       IsHitTestVisible="False"
                                       Stroke="{DynamicResource AppBorderBrush}"
                                       StrokeThickness="{DynamicResource AppBorderStrokeThickness}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ThemedTextBox" TargetType="TextBox">
            <Setter Property="Foreground" Value="{DynamicResource AppForeground}"/>
            <Setter Property="Background" Value="{DynamicResource BoxesBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}"/>
        </Style>

        <Style x:Key="ThemedListBox" TargetType="ListBox">
            <Setter Property="Foreground" Value="{DynamicResource AppForeground}"/>
            <Setter Property="Background" Value="{DynamicResource BoxesBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}"/>
        </Style>
    </UserControl.Resources>

    <DockPanel Margin="10,10,10,10">
        <GroupBox DockPanel.Dock="Bottom"
                  Header="FFmpeg Output"
                  Style="{StaticResource SectionGroupBox}"
                  Margin="0,8,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Progress:"
                           Foreground="{DynamicResource AppForeground}"
                           Margin="0,0,0,5"/>
                <ProgressBar Grid.Row="1"
                             x:Name="FFmpegProgressBar"
                             Height="16"
                             Minimum="0"
                             Maximum="100"
                             Value="0"
                             ToolTip="{StaticResource TT.Transcoding.FFmpegProgressBar}"/>

                <TextBlock Grid.Row="2"
                           Text="Output Log:"
                           Foreground="{DynamicResource AppForeground}"
                           Margin="0,10,0,5"/>
                <TextBox Grid.Row="3"
                         x:Name="FFmpegOutputBox"
                         Height="120"
                         IsReadOnly="True"
                         FontFamily="Consolas, 'Courier New', monospace"
                         FontSize="12"
                         AcceptsReturn="True"
                         TextWrapping="NoWrap"
                         HorizontalScrollBarVisibility="Auto"
                         VerticalScrollBarVisibility="Auto"
                         Style="{StaticResource ThemedTextBox}"
                         ToolTip="{StaticResource TT.Transcoding.FFmpegOutputBox}"/>
            </Grid>
        </GroupBox>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,15,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <GroupBox Grid.Row="0"
                          Header="Input File"
                          Style="{StaticResource SectionGroupBox}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="InputFilePathBox"
                                 Grid.Column="0"
                                 IsReadOnly="True"
                                 MinWidth="300"
                                 Margin="0,0,8,0"
                                 VerticalAlignment="Center"
                                 Style="{StaticResource ThemedTextBox}"
                                 ToolTip="{StaticResource TT.Transcoding.InputFilePathBox}"/>
                        <Button x:Name="BrowseInputButton"
                                Grid.Column="1"
                                Content="Browse…"
                                Width="90" Height="24"
                                VerticalAlignment="Center"
                                BorderBrush="{DynamicResource AppBorderBrush}"
                                Click="BrowseInputButton_Click"
                                ToolTip="{StaticResource TT.Transcoding.BrowseInputButton}"/>
                    </Grid>
                </GroupBox>

                <GroupBox Grid.Row="1"
                          Header="File List"
                          Style="{StaticResource SectionGroupBox}">
                    <Grid>
                        <ListBox x:Name="ExtractionOutputBox"
                                 Height="260"
                                 SelectionMode="Extended"
                                 AllowDrop="True"
                                 SelectionChanged="ExtractionOutputBox_SelectionChanged"
                                 Style="{StaticResource ThemedListBox}"
                                 ToolTip="{StaticResource TT.Transcoding.FileList}"/>
                    </Grid>
                </GroupBox>

                <GroupBox Grid.Row="2"
                          Header="Output Path"
                          Style="{StaticResource SectionGroupBox}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="OutputPathBox"
                                 Grid.Column="0"
                                 IsReadOnly="True"
                                 MinWidth="300"
                                 Margin="0,0,8,0"
                                 VerticalAlignment="Center"
                                 Style="{StaticResource ThemedTextBox}"
                                 ToolTip="{StaticResource TT.Transcoding.OutputPathBox}"/>
                        <Button x:Name="BrowseOutputButton"
                                Grid.Column="1"
                                Content="Browse…"
                                Width="90" Height="24"
                                VerticalAlignment="Center"
                                BorderBrush="{DynamicResource AppBorderBrush}"
                                Click="BrowseOutputButton_Click"
                                ToolTip="{StaticResource TT.Transcoding.BrowseOutputButton}"/>
                    </Grid>
                </GroupBox>
            </Grid>

            <StackPanel Grid.Column="1" Orientation="Vertical">
                <GroupBox Header="File Info" Style="{StaticResource SectionGroupBox}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Filename:" Foreground="{DynamicResource AppForeground}"/>
                        <TextBlock x:Name="FileNameText" Grid.Row="0" Grid.Column="1"
                                   Foreground="{DynamicResource AppForeground}"
                                   TextTrimming="CharacterEllipsis"
                                   ToolTip="{StaticResource TT.Transcoding.FileNameText}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Codec:" Foreground="{DynamicResource AppForeground}"/>
                        <TextBlock x:Name="CodecText" Grid.Row="1" Grid.Column="1"
                                   Foreground="{DynamicResource AppForeground}"
                                   ToolTip="{StaticResource TT.Transcoding.CodecText}"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Channels:" Foreground="{DynamicResource AppForeground}"/>
                        <TextBlock x:Name="ChannelsText" Grid.Row="2" Grid.Column="1"
                                   Foreground="{DynamicResource AppForeground}"
                                   ToolTip="{StaticResource TT.Transcoding.ChannelsText}"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Sample Rate:" Foreground="{DynamicResource AppForeground}"/>
                        <TextBlock x:Name="SampleRateText" Grid.Row="3" Grid.Column="1"
                                   Foreground="{DynamicResource AppForeground}"
                                   ToolTip="{StaticResource TT.Transcoding.SampleRateText}"/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Bitrate:" Foreground="{DynamicResource AppForeground}"/>
                        <TextBlock x:Name="BitrateText" Grid.Row="4" Grid.Column="1"
                                   Foreground="{DynamicResource AppForeground}"
                                   ToolTip="{StaticResource TT.Transcoding.BitrateText}"/>

                        <TextBlock Grid.Row="5" Grid.Column="0" Text="Duration:" Foreground="{DynamicResource AppForeground}"/>
                        <TextBlock x:Name="DurationText" Grid.Row="5" Grid.Column="1"
                                   Foreground="{DynamicResource AppForeground}"
                                   ToolTip="{StaticResource TT.Transcoding.DurationText}"/>

                        <TextBlock Grid.Row="6" Grid.Column="0" Text="Size:" Foreground="{DynamicResource AppForeground}"/>
                        <TextBlock x:Name="SizeText" Grid.Row="6" Grid.Column="1"
                                   Foreground="{DynamicResource AppForeground}"
                                   ToolTip="{StaticResource TT.Transcoding.SizeText}"/>
                    </Grid>
                </GroupBox>

                <GroupBox Header="Format and Gain" Style="{StaticResource SectionGroupBox}">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,6">
                            <TextBlock Text="Format:" Width="70" VerticalAlignment="Center" Foreground="{DynamicResource AppForeground}"/>
                            <ComboBox x:Name="FormatComboBox" Width="120" Height="22" Foreground="Black"
                                      ToolTip="{StaticResource TT.Transcoding.FormatComboBox}">
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem">
                                        <Setter Property="Foreground" Value="Black"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        <Setter Property="VerticalContentAlignment"   Value="Center"/>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                                <ComboBoxItem Content="wav"/>
                                <ComboBoxItem Content="wav64"/>
                                <ComboBoxItem Content="aac"/>
                                <ComboBoxItem Content="dts"/>
                                <ComboBoxItem Content="ac3"/>
                                <ComboBoxItem Content="eac3"/>
                                <ComboBoxItem Content="mp3"/>
                                <ComboBoxItem Content="flac"/>
                                <ComboBoxItem Content="ogg"/>
                                <ComboBoxItem Content="opus"/>
                            </ComboBox>
                        </DockPanel>

                        <DockPanel Margin="0,0,0,6">
                            <TextBlock Text="Bitrate:" Width="70" VerticalAlignment="Center" Foreground="{DynamicResource AppForeground}"/>
                            <ComboBox x:Name="BitrateComboBox" Width="120" Height="22" Foreground="Black"
                                      ToolTip="{StaticResource TT.Transcoding.BitrateComboBox}">
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem">
                                        <Setter Property="Foreground" Value="Black"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        <Setter Property="VerticalContentAlignment"   Value="Center"/>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                            </ComboBox>
                        </DockPanel>

                        <DockPanel Margin="0,0,0,6">
                            <TextBlock Text="Gain (dB):" Width="70" VerticalAlignment="Center" Foreground="{DynamicResource AppForeground}"/>
                            <TextBox x:Name="GainBox" Width="50" Style="{StaticResource ThemedTextBox}" Text="0"
                                     ToolTip="{StaticResource TT.Transcoding.GainBox}"/>
                        </DockPanel>

                        <CheckBox x:Name="DownmixCheckBox" Content="Downmix to Stereo"
                                  Foreground="{DynamicResource AppForeground}"
                                  ToolTip="{StaticResource TT.Transcoding.DownmixCheckBox}"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Frame Rate Conversion" Style="{StaticResource SectionGroupBox}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Orientation="Vertical">
                            <CheckBox x:Name="EnableFrameRateChangeCheckBox"
                                      Content="Enable Frame Rate Change"
                                      Foreground="{DynamicResource AppForeground}"
                                      Checked="EnableFrameRateChangeCheckBox_Checked"
                                      Unchecked="EnableFrameRateChangeCheckBox_Unchecked"
                                      Margin="0,0,0,5"
                                      ToolTip="{StaticResource TT.Transcoding.EnableFrameRateChangeCheckBox}"/>
                            <CheckBox x:Name="PreservePitchCheckbox"
                                      Content="Preserve Pitch"
                                      Foreground="{DynamicResource AppForeground}"
                                      IsEnabled="False"
                                      ToolTip="{StaticResource TT.Transcoding.PreservePitchCheckbox}"/>
                        </StackPanel>

                        <Grid Grid.Column="1" Grid.Row="0" Grid.RowSpan="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Source FPS:" VerticalAlignment="Center"
                                       Foreground="{DynamicResource AppForeground}" Margin="0,0,5,0"/>
                            <ComboBox Grid.Row="0" Grid.Column="1"
                                      x:Name="SourceFrameRateComboBox"
                                      Width="80"
                                      IsEnabled="False"
                                      Foreground="Black"
                                      ToolTip="{StaticResource TT.Transcoding.SourceFrameRateComboBox}">
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem">
                                        <Setter Property="Foreground" Value="Black"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        <Setter Property="VerticalContentAlignment"   Value="Center"/>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                                <ComboBoxItem Content="23.976"/>
                                <ComboBoxItem Content="24"/>
                                <ComboBoxItem Content="25"/>
                                <ComboBoxItem Content="29.97"/>
                                <ComboBoxItem Content="30"/>
                            </ComboBox>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Target FPS:" VerticalAlignment="Center"
                                       Foreground="{DynamicResource AppForeground}" Margin="0,0,5,0"/>
                            <ComboBox Grid.Row="1" Grid.Column="1"
                                      x:Name="TargetFrameRateComboBox"
                                      Width="80"
                                      IsEnabled="False"
                                      Foreground="Black"
                                      ToolTip="{StaticResource TT.Transcoding.TargetFrameRateComboBox}">
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem">
                                        <Setter Property="Foreground" Value="Black"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        <Setter Property="VerticalContentAlignment"   Value="Center"/>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                                <ComboBoxItem Content="23.976"/>
                                <ComboBoxItem Content="24"/>
                                <ComboBoxItem Content="25"/>
                                <ComboBoxItem Content="29.97"/>
                                <ComboBoxItem Content="30"/>
                            </ComboBox>
                        </Grid>
                    </Grid>
                </GroupBox>

                <Button x:Name="TranscodeTracksButton"
                        Content="Start Transcoding"
                        Width="220" Height="30" Margin="0,6,0,0"
                        HorizontalAlignment="Center"
                        Click="TranscodeTracksButton_Click"
                        ToolTip="{StaticResource TT.Transcoding.TranscodeTracksButton}"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</UserControl>
